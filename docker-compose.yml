services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: finastech
      POSTGRES_PASSWORD: FinasTech019284!
      POSTGRES_DB: finastech
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
